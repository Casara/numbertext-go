# Default: normalize everything to LF
* text=auto eol=lf

# Upstream data, synced verbatim byte-for-byte (see data/SOURCE.md) - never
# let git touch line endings or do any other text normalization on these.
data/*.sor -text
